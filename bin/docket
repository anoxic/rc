#!/bin/sh

if test -n "$1"; then fname=list_"$1"; else fname=list; fi
vim scp://lava/vc/qdocs/brian/"$fname"
echo commit message:
read message
ssh lava "git -C ~/vc/qdocs commit brian/list -m \"$message\""
