DOTFILES

   git clone git@github.com:anoxic/dotfiles.git
   cd ~/dotfiles && make


TODO

* create dirh, pushd, popd scripts
* add a macos config file
* make a build file for install my default utils

